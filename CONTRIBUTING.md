# Contributing to [[PROJECT_NAME]]

Thank you for your interest in contributing!

## Getting Started

1. Fork the repository and clone it locally.
2. Copy the environment file: `cp .env.sample .env`
3. Make your changes on a feature branch: `git checkout -b feat/my-feature`
4. Run `make dev` to start a local development server.

## Code Style

> Update this section with the coding conventions for this project.

## Pull Requests

- Use the pull request template provided.
- Keep PRs focused — one feature or fix per PR.
- Ensure all CI checks pass before requesting review.

## Copilot Agent Conventions

- Use `make help` to discover available targets.
- Describe your changes clearly in commit messages.
- Replace any remaining `[[TOKEN_NAME]]` placeholders before committing.
